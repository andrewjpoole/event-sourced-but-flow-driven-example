# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: event-sourced-but-flow-driven-example
metadata:
    template: azd-init@1.14.0
services:
    app-host:
        project: AppHost
        host: containerapp
        language: dotnet
